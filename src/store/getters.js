// getters: {
//         city(state) {
//             return state.city
//         }
//     }
// export const singer = state => state.singer

const getters = {
    city: (state) => {
        return state.city
    }
}

export default getters
